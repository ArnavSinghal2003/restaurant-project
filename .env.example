# Root env is optional. Keep app-specific env inside backend/.env and frontend/.env
# These entries are reference values only.

# Backend
PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/restaurant_platform
FRONTEND_URL=http://localhost:5173
JWT_SECRET=replace_with_long_secret
SESSION_TOKEN_SECRET=replace_with_another_secret

# Frontend
VITE_API_BASE_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
